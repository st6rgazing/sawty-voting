# API URL
NEXT_PUBLIC_API_BASE_URL=https://your-api-url.com

# Upstash Redis for Vercel KV (ensure tokens have trailing == as needed)
UPSTASH_REDIS_REST_URL=https://your-upstash-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token==

# Vercel KV Configuration
# These will be auto-populated by Vercel when you add the KV integration
KV_REST_API_URL=https://your-upstash-redis-url.upstash.io
KV_REST_API_TOKEN=your-upstash-redis-token==
KV_REST_API_READ_ONLY_TOKEN=your-upstash-redis-read-only-token==

# Optional: Frontend URL for generating login links
NEXT_PUBLIC_FRONTEND_URL=https://your-frontend-url.com

# Optional: Comma-separated list of emails for mass secret generation
# MAILING_LIST=email1@example.com,email2@example.com

# For local development
NODE_ENV=development 